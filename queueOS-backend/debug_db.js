const mongoose = require('mongoose'); mongoose.connect(process.env.MONGO_URL).then(async () => { const Business = require('./src/models/business.model.js'); const StaffAssignment = require('./src/models/staffAssignment.model.js'); const Token = require('./src/models/token.model.js'); console.log('Businesses:', await Business.find({}).select('businessName')); console.log('Assignments:', await StaffAssignment.find({})); console.log('Tokens:', await Token.find({})); process.exit(0); }).catch(console.error);
